typedef void (*PF)(void*);

void cobegin(PF f[], void* af[], int n){
    int* ids = new int[n];
    for(int i=0; i<n; i++){
        if((ids[i] = fork()) == 0) {
            pf[i](af[i]);
            exit();
        }
    }
    for(int i=0; i < n; i++){
        wait(ids[i]);
    }
    delete ids;

}